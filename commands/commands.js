module.exports = {
    name: 'commands',
    description: "commands",
    execute(message, args){
        message.channel.send('ban, clear, commands, hello, kick, mute, ping, unmute');

        } 
        
    }
